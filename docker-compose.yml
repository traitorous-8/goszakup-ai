services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ows
      POSTGRES_USER: ows
      POSTGRES_PASSWORD: ows_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: